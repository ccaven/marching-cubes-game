
class World {

    constructor () {

        this.chunks = [];
        this.chunkReference = {};
        this.loadQueue = [];

    }

    addChunkToLoadingQueue (x, y) {

    }

    fillLoadingQueue () {
        let s = 5;

        for (let dx = -s; dx <= s; dx ++) {
            for (let dy = -s; dy <= s; dy ++) {




            }
        }
    }

}
